spring:
  datasource:
    url: jdbc:tc:postgresql:15:///testdb
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true